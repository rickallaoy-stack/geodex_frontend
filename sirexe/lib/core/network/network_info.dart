/// Placeholder réseau. Pour un vrai check de connectivité,
/// remplacer par `connectivity_plus` ou `internet_connection_checker`.
class NetworkInfo {
  Future<bool> get isConnected async => true;
}
